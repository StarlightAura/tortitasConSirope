version: "3"
services:
  springapp:
    build: .
    container_name: springapp
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://db:5432/games
    ports:
      - 8000:8080
    restart: always
    depends_on:
      - db
  db:
    image: postgres
    container_name: postgres_tfg
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=010318
      - POSTGRES_DB=games
      - PGDATA=/var/lib/postgresql
    ports:
      - 5000:5432
    volumes:
      - pgdata:/var/lib/postgresql
    restart: always
volumes:
  pgdata: